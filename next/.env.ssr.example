# Runtime environment values for the Angular SSR server (next/src/server.ts)
# Copy this file to .env.ssr and fill in real values.

PREVIEW_SECRET=replace_with_same_value_as_strapi_preview_secret
PORT=4000
